# Copy to .env and fill in. .env is gitignored.
# DataForSEO Basic Auth: base64 of "login:password" (used directly as the
# "Authorization: Basic <value>" header). Used by the pipeline for keyword research.
DATAFORSEO_AUTH_B64=
